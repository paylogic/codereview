language: python
python:
    - 2.7
# command to install dependencies
# # command to run tests
install: true
script:
  - make test python_version=2.7
after_success:
  - make coveralls
notifications:
    email:
        - opensource-tests@paylogic.com
